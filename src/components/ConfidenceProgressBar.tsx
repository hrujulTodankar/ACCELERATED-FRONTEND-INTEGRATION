import React from 'react'; interface ConfidenceProgressBarProps { confidence: number; size?: 'sm' | 'md' | 'lg'; showLabel?: boolean; animated?: boolean; } const ConfidenceProgressBar: React.FC<ConfidenceProgressBarProps> = ({ confidence, size = 'md', showLabel = false, animated = true }) => { const getSizeClasses = () => { switch (size) { case 'sm': return 'h-2'; case 'lg': return 'h-4'; default: return 'h-3'; } }; const getBarColor = () => { if (confidence >= 0.8) return 'bg-green-500'; if (confidence >= 0.6) return 'bg-yellow-500'; return 'bg-red-500'; }; const getTextColor = () => { if (confidence >= 0.8) return 'text-green-700'; if (confidence >= 0.6) return 'text-yellow-700'; return 'text-red-700'; }; const getBadgeColor = () => { if (confidence >= 0.8) return 'bg-green-100 text-green-800'; if (confidence >= 0.6) return 'bg-yellow-100 text-yellow-800'; return 'bg-red-100 text-red-800'; }; return ( <div className="flex items-center space-x-2"><div className="flex-1 min-w-0"><div className={`w-full bg-gray-200 rounded-full ${getSizeClasses()}`}><div className={`${getSizeClasses()} ${getBarColor()} rounded-full transition-all duration-500 ease-out ${ animated ? 'animate-pulse' : '' }`} style={{ width: `${Math.max(confidence * 100, 5)}%` }} ></div></div></div> {showLabel && ( <span className={`text-xs font-medium whitespace-nowrap ${getTextColor()}`}> {(confidence * 100).toFixed(0)}% </span> )} </div> ); }; export default ConfidenceProgressBar;